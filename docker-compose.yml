services:

  auth-api:
    build: ./auth-api
    ports:
      - "8080:8080"

  b2b-inventory-api:
    build: ./b2b-inventory-api
    ports:
      - "8081:8081"
    environment:
      - AUTH_API_URL=http://auth-api:8080

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
